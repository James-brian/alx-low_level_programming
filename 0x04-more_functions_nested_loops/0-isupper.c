#include "main.h"
#include <stdio.h>

/**
 * _isupper - check for upper case letter
 *
 * @x : character to check the case
 *
 * Return: 0 or 1.
 */

int _isupper(int x)
{
return (x >= 65 && x <= 90);
}
